---
all:
  children:
    clients:
      hosts:
        example-client:
          ansible_host: 203.0.113.10
          ansible_user: deploy
          ansible_ssh_private_key_file: ~/.ssh/clients/example-client
